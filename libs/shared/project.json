{
  "name": "shared",
  "projectType": "library",
  "sourceRoot": "libs/shared/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{projectRoot}/dist"],
      "options": {
        "command": "pnpm exec tsc -p libs/shared/tsconfig.build.json"
      }
    },
    "typecheck": {
      "cache": true
    }
  }
}
