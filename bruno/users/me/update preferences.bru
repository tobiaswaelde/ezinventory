meta {
  name: update preferences
  type: http
  seq: 2
}

patch {
  url: {{host}}/api/v1/users/me/preferences
  body: json
  auth: inherit
}

body:json {
  {
    "language": "en-US"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
