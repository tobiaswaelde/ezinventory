meta {
  name: update
  type: http
  seq: 1
}

post {
  url: {{host}}/api/v1/auth/password/update
  body: json
  auth: inherit
}

body:json {
  {
    "currentPassword": "{{password}}",
    "newPassword": "{{password}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
