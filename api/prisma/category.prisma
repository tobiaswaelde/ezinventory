model Category {
  id          String   @id @default(uuid()) @db.Uuid
  name        String   @unique
  description String?
  iconSet     IconSet?
  iconName    String?

  items Item[]

  @@map("categories")
}
